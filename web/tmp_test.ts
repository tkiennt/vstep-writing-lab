import axios from 'axios';

async function testFetch() {
    try {
        console.log("This will need the auth token of the user, which we don't have easily.");
    } catch(e) {
        console.error(e);
    }
}
testFetch();
